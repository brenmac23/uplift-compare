---
phase: 05-generator-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - scripts/generator/index.ts
  - scripts/generateSeedData.ts
  - package.json
  - src/data/seedProjects.ts
autonomous: true
requirements: [GEN-02, GEN-03, GEN-04]

must_haves:
  truths:
    - "Running npm run seed completes without error and writes src/data/seedProjects.ts"
    - "Running npm run seed twice in a row produces an identical file"
    - "The app compiles after seed regeneration — no TypeScript errors"
    - "Generator prints a distribution summary to stdout after each run"
    - "scoreExisting is called on each generated project to track running scores"
  artifacts:
    - path: "scripts/generator/index.ts"
      provides: "generateProject() function assembling one Project from PRNG"
      exports: ["generateProject"]
    - path: "scripts/generateSeedData.ts"
      provides: "Entry point: orchestrates generation, writes file, prints report"
      min_lines: 40
    - path: "src/data/seedProjects.ts"
      provides: "Auto-generated 50-project seed data file"
      contains: "export const SEED_PROJECTS"
    - path: "package.json"
      provides: "npm run seed script"
      contains: "seed"
  key_links:
    - from: "scripts/generator/index.ts"
      to: "scripts/generator/prng.ts"
      via: "receives rand function parameter"
      pattern: "rand.*number"
    - from: "scripts/generator/index.ts"
      to: "src/scoring/scoreExisting.ts"
      via: "imports scoreExisting for running score"
      pattern: "import.*scoreExisting"
    - from: "scripts/generateSeedData.ts"
      to: "scripts/generator/index.ts"
      via: "calls generateProject in a loop"
      pattern: "generateProject"
    - from: "scripts/generateSeedData.ts"
      to: "src/data/seedProjects.ts"
      via: "writeFileSync output"
      pattern: "writeFileSync"
---

<objective>
Create the generateProject() function, entry point script, distribution report, and npm run seed command. Wire all Plan 01 building blocks into a working generator that produces a valid seedProjects.ts.

Purpose: Deliver the complete working generator — the main deliverable of Phase 5. After this plan, `npm run seed` produces deterministic, type-correct output that passes all existing tests.
Output: Working `npm run seed` command, auto-generated seedProjects.ts, distribution report on stdout.
</objective>

<execution_context>
@C:/Users/New User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/New User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-generator-infrastructure/05-RESEARCH.md
@.planning/phases/05-generator-infrastructure/05-01-SUMMARY.md

<interfaces>
<!-- Types created by Plan 01 that this plan uses -->

From scripts/generator/types.ts:
```typescript
export type ProductionType = 'film' | 'tv';
export type BudgetTier = 'small' | 'mid' | 'large' | 'tentpole';
export interface BudgetTierConfig { tier: BudgetTier; minQnzpe: number; maxQnzpe: number; clusterCenter: number; }
export interface GeneratorConfig { seed: number; projectCount: number; }
export const PROJECT_COUNT = 50;
export const SEED = 0xDEADBEEF;
```

From scripts/generator/prng.ts:
```typescript
export function createPrng(seed: number): () => number;
```

From scripts/generator/projectNames.ts:
```typescript
export const FILM_NAMES: string[];  // ~48 film titles
export const TV_NAMES: string[];    // ~32 TV titles
```

From scripts/generator/tiers.ts:
```typescript
export const BUDGET_TIERS: BudgetTierConfig[];
export function sampleQnzpe(tier: BudgetTierConfig, rand: () => number): number;
export function pickBudgetTier(rand: () => number): BudgetTierConfig;
```

From src/scoring/index.ts:
```typescript
export { scoreExisting } from './scoreExisting';
export type { ProjectInputs, ScoringResult } from './types';
```

From src/store/useAppStore.ts:
```typescript
export interface Project { id: string; isSeeded: boolean; inputs: ProjectInputs; createdAt: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generateProject() and entry point script</name>
  <files>scripts/generator/index.ts, scripts/generateSeedData.ts, package.json</files>
  <action>
1. Create `scripts/generator/index.ts` with `generateProject()`:
   - Signature: `export function generateProject(rand: () => number, index: number, name: string, productionType: ProductionType, tierConfig: BudgetTierConfig): { project: Project; existingScore: number }`
   - Import `scoreExisting` from `../../src/scoring/scoreExisting`
   - Import `ProjectInputs` from `../../src/scoring/types`
   - Import `sampleQnzpe` from `./tiers`
   - Build a `ProjectInputs` object:
     - `projectName`: from the `name` parameter
     - `productionType`: from the parameter
     - `qnzpe`: call `sampleQnzpe(tierConfig, rand)`
     - `hasSustainabilityPlan: true` (always — test requires 100%)
     - `maoriCrewPercent: 0` (always — test requires; Phase 7 handles SCEN-02)
     - `hasLeadCastMaori: false` (always — test requires; Phase 7 handles)
     - For all other fields, generate placeholder values using `rand()`:
       - Boolean fields: `rand() < probability` — use reasonable defaults (e.g., hasPreviousQNZPE: rand() < 0.5, hasStudioLease: rand() < 0.08 to hit 3-5 out of 50)
       - Percentage fields: `Math.round(rand() * 100)` for most; crewPercent should be `80 + Math.round(rand() * 20)` to ensure >= 80% for at least 40 projects (test requirement)
       - Count fields: `Math.floor(rand() * (max + 1))`
       - castingLevel: pick from ['none', 'associate', 'director'] based on rand()
       - premiereType: pick from ['none', 'nz', 'world'] based on rand()
       - Section E fields (hasKnowledgeTransfer, commercialAgreementPercent, infrastructureInvestment): only allow non-zero when qnzpe >= $100m (test requirement), and keep total Section E active count sparse (rand() < 0.15 for each)
     - NOTE: These are placeholder values for Phase 5. Phase 6 replaces them with correlated tier-based logic. The goal here is to produce values that pass the existing test assertions, not to be realistic.
   - After building inputs, call `scoreExisting(inputs)` and extract `totalPoints` (GEN-02: running score tracking)
   - Return `{ project: { id: \`seed-\${String(index + 1).padStart(3, '0')}\`, isSeeded: true, createdAt: '2026-01-01T00:00:00.000Z', inputs }, existingScore }`

2. Create `scripts/generateSeedData.ts` (entry point):
   - Import: `createPrng` from `./generator/prng`, `SEED, PROJECT_COUNT` from `./generator/types`, `FILM_NAMES, TV_NAMES` from `./generator/projectNames`, `pickBudgetTier` from `./generator/tiers`, `generateProject` from `./generator/index`
   - Import `writeFileSync` from `fs`, `resolve` from `path`
   - Create PRNG: `const rand = createPrng(SEED)`
   - Determine production types for all 50 projects: `rand() < 0.6 ? 'film' : 'tv'`
   - Count how many film vs TV, then shuffle film names and TV names with PRNG (Fisher-Yates), take the needed count from each pool
   - Generate all 50 projects by calling `generateProject()` in a loop, collecting results
   - Print distribution report to stdout (GEN-03) — format:
     ```
     === Seed Data Distribution Report ===
     Total projects: 50
     Film: NN | TV: NN
     Budget tiers: small=NN mid=NN large=NN tentpole=NN
     Pass rate (existing): NN/50 (NN%)
     Score range: NN-NN
     Borderline (38-42): NN
     hasSustainabilityPlan=true: 50/50
     maoriCrewPercent=0: 50/50
     hasStudioLease=true: NN
     Section E active: NN
     crewPercent>=80: NN
     qnzpe>=$100m: NN
     =====================================
     ```
   - Write the output file using template literal code generation (GEN-04):
     - Output path: `resolve(import.meta.dirname, '../src/data/seedProjects.ts')`
     - Header: auto-generated comment with seed constant, import of Project type
     - Body: each project rendered with `JSON.stringify(project.inputs, null, 6)` for the inputs object, wrapped in the Project object literal
     - Export: `export const SEED_PROJECTS: Project[] = [...]`
   - Print "Written: {path}" confirmation

3. Add to package.json scripts: `"seed": "tsx scripts/generateSeedData.ts"`
  </action>
  <verify>
    <automated>cd "C:/Users/New User/Dropbox/claude/uplift" && npm run seed && npx vitest run src/data/__tests__/seedProjects.test.ts</automated>
  </verify>
  <done>npm run seed writes src/data/seedProjects.ts with 50 projects. All 12 existing seedProjects.test.ts assertions pass. Distribution report printed to stdout. scoreExisting called on each project for running score tracking.</done>
</task>

<task type="auto">
  <name>Task 2: Verify determinism and TypeScript compilation</name>
  <files>src/data/seedProjects.ts</files>
  <action>
1. Verify determinism (GEN-01): Run `npm run seed` twice and diff the output.
   - `npm run seed` (first run)
   - Copy `src/data/seedProjects.ts` to a temp location
   - `npm run seed` (second run)
   - Diff the two files — they must be identical
   - If not identical, debug PRNG call order or any source of non-determinism (e.g., Date.now(), object key ordering)

2. Verify TypeScript compilation: Run `npx tsc --noEmit` to confirm the generated file has no type errors and the app compiles correctly.

3. Run the full test suite: `npm test` to confirm no regressions anywhere.

4. If any test fails, adjust the probability constants in generateProject() to satisfy the test constraints:
   - 50 projects (exact)
   - 20-30 pass scoreExisting
   - 20-30 have qnzpe >= $100m
   - At least 5 borderline (38-42pts)
   - 3-5 have hasStudioLease
   - All have hasSustainabilityPlan=true, maoriCrewPercent=0, hasLeadCastMaori=false
   - Section E only on qnzpe >= $100m, max 5-8 with any Section E active
   - At least 40 have crewPercent >= 80
   - At least 15 film and 15 TV

   Tuning approach: Adjust probability thresholds (e.g., hasStudioLease probability, Section E probability, boolean field probabilities) until all tests pass. Do NOT change the PRNG seed — change the probabilities that consume the PRNG values.
  </action>
  <verify>
    <automated>cd "C:/Users/New User/Dropbox/claude/uplift" && npm run seed && cp src/data/seedProjects.ts /tmp/seed1.ts && npm run seed && diff /tmp/seed1.ts src/data/seedProjects.ts && npx tsc --noEmit && npm test</automated>
  </verify>
  <done>npm run seed produces identical output on consecutive runs (deterministic). TypeScript compiles without errors. Full test suite passes with no regressions.</done>
</task>

</tasks>

<verification>
- `npm run seed` completes without error
- `npm run seed` output includes "=== Seed Data Distribution Report ===" with pass rate, score range, and tier breakdown
- Generated `src/data/seedProjects.ts` starts with auto-generated comment and exports `SEED_PROJECTS`
- `npx vitest run src/data/__tests__/seedProjects.test.ts` — all 12 assertions pass
- Two consecutive runs of `npm run seed` produce identical files
- `npx tsc --noEmit` passes
- `npm test` passes (full suite, no regressions)
</verification>

<success_criteria>
- `npm run seed` is a working command that generates 50 deterministic projects
- Output file is valid TypeScript that the app compiles against
- Distribution report appears on stdout with all key metrics
- All existing seedProjects.test.ts assertions pass
- Determinism verified: identical output from identical seed
- No TypeScript compilation errors
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-generator-infrastructure/05-02-SUMMARY.md`
</output>

---
phase: 06-tiered-field-logic
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/__tests__/seedProjects.test.ts
  - scripts/generator/correlations.ts
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04, DIST-05]

must_haves:
  truths:
    - "Distribution test assertions exist for all five DIST requirements before any generation logic changes"
    - "Correlations module exports budget-inverse weight tables for qualifying person fields"
    - "Correlations module exports bimodal sampling parameters for post-production fields"
  artifacts:
    - path: "src/data/__tests__/seedProjects.test.ts"
      provides: "Distribution assertions for DIST-01 through DIST-05"
      contains: "bimodal"
    - path: "scripts/generator/correlations.ts"
      provides: "Weight tables and correlation constants for tier generation"
      exports: ["BUDGET_INVERSE_ATL_WEIGHTS", "BUDGET_INVERSE_BTL_WEIGHTS", "POST_PRODUCTION_CONFIG", "B4_C2_COVARIANCE"]
  key_links:
    - from: "scripts/generator/correlations.ts"
      to: "scripts/generator/types.ts"
      via: "BudgetTier type import"
      pattern: "import.*BudgetTier"
---

<objective>
Add distribution test assertions (Wave 0 Nyquist compliance) and populate the correlations module with weight tables that Tier 1/2/3 generation will consume.

Purpose: Tests must exist before implementation to catch regressions during the tier refactor. The correlations module is the data backbone that all three tier modules will import.
Output: Failing tests (expected — generation logic not yet updated), populated correlations.ts with exported weight tables.
</objective>

<execution_context>
@C:/Users/New User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/New User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tiered-field-logic/6-CONTEXT.md
@.planning/phases/06-tiered-field-logic/06-RESEARCH.md

@scripts/generator/correlations.ts
@scripts/generator/types.ts
@src/data/__tests__/seedProjects.test.ts
@src/scoring/types.ts

<interfaces>
<!-- Key types the executor needs -->

From scripts/generator/types.ts:
```typescript
export type BudgetTier = 'small' | 'mid' | 'large' | 'tentpole';
export type ProductionType = 'film' | 'tv';
export interface BudgetTierConfig {
  tier: BudgetTier;
  minQnzpe: number;
  maxQnzpe: number;
  clusterCenter: number;
}
```

From scripts/generator/tiers.ts:
```typescript
export const BUDGET_TIERS: BudgetTierConfig[] = [
  { tier: 'small',    minQnzpe:  20_000_000, maxQnzpe:  49_999_999, clusterCenter: 0.4  },
  { tier: 'mid',      minQnzpe:  50_000_000, maxQnzpe:  99_999_999, clusterCenter: 0.35 },
  { tier: 'large',    minQnzpe: 100_000_000, maxQnzpe: 199_999_999, clusterCenter: 0.3  },
  { tier: 'tentpole', minQnzpe: 200_000_000, maxQnzpe: 250_000_000, clusterCenter: 0.4  },
];
```

From src/data/__tests__/seedProjects.test.ts (existing test structure):
- Imports SEED_PROJECTS from '../seedProjects' and scoreExisting from '../../scoring'
- Uses describe/it/expect from vitest
- Already has 12 test cases covering shape, budget, scoring, and realism rules
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add distribution test assertions for DIST-01 through DIST-05</name>
  <files>src/data/__tests__/seedProjects.test.ts</files>
  <action>
Add a new describe block "distribution correlations (DIST-01 through DIST-05)" inside the existing top-level describe, after the existing tests. Add these test cases:

**DIST-01 (bimodal post-production):**
- `it('picturePostPercent is bimodal — all values either <= 10 or >= 75')` — iterate all 50 projects, assert each `p.inputs.picturePostPercent` is either `<= 10` or `>= 75`. Same assertion for `soundPostPercent`.
- `it('at least 40% of projects have high post-production (picturePostPercent >= 75)')` — count projects with picturePostPercent >= 75, assert count >= 20.

**DIST-02 (VFX/concept independence):**
- `it('vfxPercent and conceptPhysicalPercent vary independently from post-production cluster')` — split projects into highPost (picturePostPercent >= 75) and lowPost groups. For each group, compute mean vfxPercent. Assert the difference between means is less than 30 (they should not be tightly coupled — a 30-point gap would indicate coupling).

**DIST-03 (C6 >= C5 constraint):**
- `it('btlAdditionalCount >= btlKeyCount for all projects')` — iterate all 50, assert `p.inputs.btlAdditionalCount >= p.inputs.btlKeyCount`.
- `it('btlKeyCount === 0 implies btlAdditionalCount === 0')` — iterate all 50, if btlKeyCount is 0, assert btlAdditionalCount is 0.

**DIST-04 (B4/C2 co-variance):**
- `it('low shooting percent correlates with lower crew percent pass rate')` — split into lowShooting (shootingNZPercent < 75) and highShooting (>= 90). Compute fraction with crewPercent >= 80 in each group. Assert highShooting fraction > lowShooting fraction.

**DIST-05 (budget-inverse qualifying person):**
- `it('higher budget tiers have lower average atlCount')` — split into smallMid (qnzpe < 100m) and largeTentpole (qnzpe >= 100m). Compute mean atlCount for each. Assert smallMid mean > largeTentpole mean.

These tests WILL FAIL until Plan 02/03 implement the correlated generation logic. That is expected and correct for Wave 0.
  </action>
  <verify>
    <automated>npx vitest run src/data/__tests__/seedProjects.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>New distribution tests added. Some or all of the new DIST tests may fail (expected — Phase 5 placeholder logic does not satisfy all distribution constraints). Existing 12 tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Populate correlations module with weight tables and sampling constants</name>
  <files>scripts/generator/correlations.ts</files>
  <action>
Replace the empty `CORRELATIONS = {}` stub with specific exported constants that tier1.ts, tier2.ts, and tier3.ts will import in Plans 02 and 03.

**Export these constants:**

1. `BUDGET_INVERSE_ATL_WEIGHTS: Record<BudgetTier, number[]>` — probability weights for atlCount values [0, 1, 2, 3] per budget tier. Small/mid favor higher counts, large/tentpole favor lower counts. Use values from research:
   - small: [0.10, 0.25, 0.35, 0.30]
   - mid: [0.15, 0.30, 0.35, 0.20]
   - large: [0.25, 0.35, 0.25, 0.15]
   - tentpole: [0.40, 0.35, 0.15, 0.10]

2. `BUDGET_INVERSE_CAST_WEIGHTS: Record<BudgetTier, number>` — probability of hasLeadCast being true per budget tier. Higher budget = lower probability of local lead cast.
   - small: 0.85, mid: 0.75, large: 0.60, tentpole: 0.45

3. `BUDGET_INVERSE_SUPPORTING_CAST_WEIGHTS: Record<BudgetTier, number[]>` — probability weights for supportingCastCount [0, 1, 2, 3] per budget tier. Same inverse pattern as ATL.
   - small: [0.10, 0.20, 0.35, 0.35]
   - mid: [0.15, 0.25, 0.35, 0.25]
   - large: [0.25, 0.30, 0.30, 0.15]
   - tentpole: [0.35, 0.35, 0.20, 0.10]

4. `CASTING_LEVEL_WEIGHTS: Record<BudgetTier, { director: number; associate: number }>` — cumulative thresholds for casting level per budget tier. Smaller budgets more likely to use local casting director.
   - small: { director: 0.30, associate: 0.65 }
   - mid: { director: 0.20, associate: 0.55 }
   - large: { director: 0.15, associate: 0.45 }
   - tentpole: { director: 0.10, associate: 0.35 }

5. `POST_PRODUCTION_CONFIG` — object with `highLowSplitProb: 0.5` (coin flip for high/low cluster), `splitProb: 0.10` (10% chance picture and sound diverge), `highRange: { min: 85, max: 100 }`, `lowRange: { min: 0, max: 5 }`.

6. `B4_C2_COVARIANCE` — object with `highShootingThreshold: 75`, `highShootingCrewPassProb: 0.88`, `lowShootingCrewPassProb: 0.35`.

7. `BTL_CONFIG` — object defining C5/C6 rules:
   - `c5MaxWithPost: 7` (B6/B7 high expands C5 range)
   - `c5MaxWithoutPost: 4` (no NZ post = smaller range)
   - `c6Max: 8`
   - `budgetC5Bias: Record<BudgetTier, number>` — fraction of c5Max that serves as the effective ceiling. tentpole: 0.4 (skews low), small: 0.9 (uses full range).

8. `AMBITION_TARGET_WEIGHTS: number[]` — weights for ambition targets [40, 41, 42, 43, 44, 45]. From research: [0.15, 0.15, 0.20, 0.25, 0.15, 0.10].

9. `TIER3_FIELD_COSTS` — array of `{ field: keyof ProjectInputs; value: unknown; points: number; cost: 'cheap' | 'medium' | 'expensive' }` ordered by cost ascending. Cheap: D2 (hasEdSeminars, 1pt), D1 (hasMasterclass, 2pt), A2 (hasSustainabilityOfficer, 2pt), A3 (hasCarbonReview, 2pt), D3 (attachmentCount, 2pt), D4 (internshipCount, 1pt). Medium: F1-nz (premiereType nz, 2pt), F1-world (premiereType world, 3pt), F2 (hasFilmMarketing, 3pt), F3 (hasTourismMarketing, 3pt), F4 (hasTourismPartnership, 3pt). Expensive: E1 (hasKnowledgeTransfer, 2pt), E2 (commercialAgreementPercent, up to 3pt), E3 (infrastructureInvestment, up to 3pt).

Remove the old `CORRELATIONS = {}` export and the `_EnsureImportsUsed` type hack. Import `BudgetTier` as a value-relevant type (used in Record keys). Also import `ProjectInputs` from `../../src/scoring/types` for typing the TIER3_FIELD_COSTS array.

Keep comments concise — one line per constant explaining its purpose.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>correlations.ts exports all weight tables and configuration constants. TypeScript compiles cleanly. No runtime behavior change yet — these are data tables consumed by Plans 02 and 03.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (correlations.ts type-checks)
- Existing 12 seedProjects tests still pass
- New DIST tests exist (some may fail — expected for Wave 0)
- correlations.ts exports are importable (verified by tsc)
</verification>

<success_criteria>
- Distribution test assertions cover all 5 DIST requirements
- correlations.ts exports at least 8 named constants with proper BudgetTier typing
- No regression in existing test suite (263 tests)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-tiered-field-logic/06-01-SUMMARY.md`
</output>

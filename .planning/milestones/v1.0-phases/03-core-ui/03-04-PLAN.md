---
phase: 03-core-ui
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/components/CreateProjectModal.tsx
  - src/components/ImportButton.tsx
  - src/components/NavBar.tsx
  - src/store/useAppStore.ts
autonomous: false
requirements: [PROJ-09, DISP-02]

must_haves:
  truths:
    - "New Project button in nav opens a modal dialog with name, QNZPE, and production type fields"
    - "After creating a project, the modal closes and app navigates to the new project's detail screen"
    - "Import button opens a file picker for .json files"
    - "Valid JSON matching ProjectInputs shape is imported as a new project"
    - "Invalid JSON shows an error message to the user"
    - "The app looks polished with a modern light theme — warmer whites, rounded cards, consistent spacing"
  artifacts:
    - path: "src/components/CreateProjectModal.tsx"
      provides: "Modal dialog for creating new projects"
      exports: ["CreateProjectModal"]
    - path: "src/components/ImportButton.tsx"
      provides: "JSON file import with validation"
      exports: ["ImportButton"]
    - path: "src/store/useAppStore.ts"
      provides: "Updated addProject that accepts optional pre-generated ID"
  key_links:
    - from: "src/components/NavBar.tsx"
      to: "src/components/CreateProjectModal.tsx"
      via: "renders modal triggered by New Project button"
      pattern: "CreateProjectModal"
    - from: "src/components/NavBar.tsx"
      to: "src/components/ImportButton.tsx"
      via: "renders import button in nav"
      pattern: "ImportButton"
    - from: "src/components/CreateProjectModal.tsx"
      to: "src/store/useAppStore.ts"
      via: "addProject call with pre-generated UUID"
      pattern: "addProject"
---

<objective>
Wire the New Project modal and JSON import button into the nav bar, update the store to support pre-generated IDs for navigation after creation, and verify the complete UI visually.

Purpose: Completes all interactive features. After this plan, the app is fully functional with all Phase 3 requirements met.
Output: Working create modal, JSON import, and polished visual design confirmed by human review.
</objective>

<execution_context>
@C:/Users/New User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/New User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-ui/03-CONTEXT.md
@.planning/phases/03-core-ui/03-RESEARCH.md
@.planning/phases/03-core-ui/03-01-SUMMARY.md
@.planning/phases/03-core-ui/03-02-SUMMARY.md
@.planning/phases/03-core-ui/03-03-SUMMARY.md

@src/store/useAppStore.ts
@src/components/NavBar.tsx
@src/components/CreateProjectForm.tsx
@src/scoring/types.ts

<interfaces>
From src/store/useAppStore.ts:
```typescript
export interface Project {
  id: string;
  isSeeded: boolean;
  inputs: ProjectInputs;
  createdAt: string;
}

interface AppState {
  addProject: (inputs: ProjectInputs) => void;
  // ... other actions
}
```

From src/components/CreateProjectForm.tsx (existing logic to reuse):
```typescript
// Form has: projectName (string), qnzpe (string parsed to number * 1_000_000), productionType ('film' | 'tv')
// All other ProjectInputs fields default to zero/false/none
// Validation: name required, qnzpe must be positive number
```

From src/scoring/types.ts:
```typescript
export interface ProjectInputs {
  projectName: string;
  productionType: 'film' | 'tv';
  qnzpe: number;
  // ... 30+ other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update store, create modal and import, wire into NavBar</name>
  <files>src/store/useAppStore.ts, src/components/CreateProjectModal.tsx, src/components/ImportButton.tsx, src/components/NavBar.tsx</files>
  <action>
**1. Update src/store/useAppStore.ts — addProject to accept optional ID:**

Change `addProject` signature to accept an optional pre-generated ID:
```typescript
addProject: (inputs: ProjectInputs, id?: string) => void;
```
Implementation: use `id ?? crypto.randomUUID()` for the project ID. This lets the modal generate the ID before calling addProject, then navigate to that ID. See 03-RESEARCH.md Pitfall 4 and Open Question 1.

**2. Create src/components/CreateProjectModal.tsx:**

Uses shadcn Dialog (see 03-RESEARCH.md Pattern 6):
- Props: `{ open: boolean; onOpenChange: (open: boolean) => void }`
- Reuse the form logic from existing CreateProjectForm.tsx:
  - Three fields: project name (text input), QNZPE in $M (number input), production type (select dropdown)
  - Same validation: name required, QNZPE must be positive
  - Same defaults: all scoring fields default to zero/false/none
- On submit:
  1. Generate UUID with `crypto.randomUUID()`
  2. Call `addProject(inputs, generatedId)`
  3. Close the dialog via `onOpenChange(false)`
  4. Navigate to `/project/${generatedId}` using useNavigate
- Use shadcn Dialog, DialogContent, DialogHeader, DialogTitle
- Use shadcn Button for submit
- Use shadcn Select for production type (shadcn Select, not native select)
- Style form inputs with Tailwind: proper spacing, labels, error messages

**3. Create src/components/ImportButton.tsx:**

JSON file import (see 03-RESEARCH.md Pattern 7):
- Renders a shadcn Button labelled "Import" that triggers a hidden file input
- File input accepts `.json` files only: `accept=".json"`
- On file selection:
  1. Read file with FileReader
  2. Parse JSON
  3. Validate against ProjectInputs shape using `isValidProjectInputs()` function:
     - Check it's an object
     - Check required fields exist with correct types: projectName (string), qnzpe (number), productionType ('film' | 'tv')
     - Spot-check a few more: hasSustainabilityPlan (boolean), castPercent (number)
     - Fill in missing optional fields with defaults (zero/false/none) — be lenient on import
  4. If valid: generate UUID, call `addProject(validatedInputs, id)`, navigate to `/project/${id}`
  5. If invalid: show error message (use a simple alert or toast)
- Props: none (uses useAppStore and useNavigate internally)

**4. Update src/components/NavBar.tsx:**

Wire the placeholder buttons to real components:
- Import CreateProjectModal, manage `open` state for the dialog
- "New Project" button triggers `setOpen(true)`, renders CreateProjectModal with open/onOpenChange
- Replace placeholder "Import" button with ImportButton component
- Keep existing nav layout and styling

**5. Delete src/components/CreateProjectForm.tsx** — no longer needed, replaced by CreateProjectModal.

**6. Verify the complete flow:**
- Click "New Project" -> modal opens -> fill form -> submit -> navigates to new project detail
- Click "Import" -> file picker opens -> select valid JSON -> navigates to imported project detail
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>New Project modal creates project and navigates to it. Import button accepts JSON file and creates project. NavBar wired with both actions. Old CreateProjectForm deleted. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of complete Phase 3 UI</name>
  <files>none</files>
  <action>
Human verifies the complete Phase 3 UI by following the verification steps below. No code changes — this is a visual and functional review checkpoint.
  </action>
  <what-built>Complete Phase 3 UI: summary screen with filters and stat cards, detail screen with three-column live scoring layout, tooltips, collapsible sections, create project modal, JSON import, and visual theme.</what-built>
  <how-to-verify>
1. Open `npm run dev` in browser

2. **Summary Screen (/):**
   - Verify 50 projects listed with name, type, QNZPE, existing score + pass/fail, proposed score + pass/fail
   - Verify pass/fail badges are blue (PASS) and orange (FAIL) with text labels
   - Click a stat card (e.g. "Existing Pass") — table should filter, stats should update
   - Use filter dropdowns to filter by type (Film/TV) and budget tier
   - Click "Clear Filters" to reset

3. **Detail Screen (click any project row):**
   - Verify three-column layout: inputs | existing scores | proposed scores
   - Change an input (e.g. toggle hasSustainabilityPlan checkbox) — scores should update instantly
   - Verify section alignment: Sustainability section shows "No equivalent" placeholder in proposed column
   - Hover over a criterion "?" icon — tooltip should explain the rule
   - Click a section header — section should collapse/expand
   - Use project switcher dropdown — should navigate to a different project
   - Verify sticky score headers stay visible while scrolling

4. **New Project:**
   - Click "New Project" in nav bar — modal should open
   - Fill in name, QNZPE, type, submit — should navigate to new project's detail screen
   - Go back to summary — new project should appear in table

5. **Import:**
   - Click "Import" in nav bar — file picker should open
   - (If you have a test JSON file: import it and verify it appears)

6. **Visual Polish:**
   - Overall appearance: modern light theme, warmer whites, rounded cards, shadows
   - Typography and spacing look professional
   - Responsive: resize browser window to check mobile layout

Type "approved" if everything works, or describe any issues.
  </how-to-verify>
  <verify>Human confirms all screens render correctly and interactions work as expected.</verify>
  <done>Human has approved the complete Phase 3 UI — all screens functional, visual design polished, all requirements met.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- Full user flow works: summary -> click project -> detail with live scoring -> back to summary
- Create project modal -> navigates to new project
- Import JSON -> creates and navigates to project
- Visual theme is polished and accessible
</verification>

<success_criteria>
- New Project modal creates project and navigates to detail
- JSON import validates and creates project
- All PROJ and DISP requirements visually confirmed
- App looks polished with consistent design system
- Human approval checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-ui/03-04-SUMMARY.md`
</output>

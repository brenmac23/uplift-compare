---
phase: 02-data-layer
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/App.tsx
  - src/main.tsx
  - src/pages/ProjectListPage.tsx
  - src/components/CreateProjectForm.tsx
  - netlify.toml
  - package.json
autonomous: false
requirements: [INFRA-01, PROJ-04]

must_haves:
  truths:
    - "App uses HashRouter so all routes work on Netlify without redirect config"
    - "Root route (/#/) renders a project list showing all 50 seed projects"
    - "App builds successfully with npm run build"
    - "App deploys to Netlify and loads with 50 projects visible"
    - "User can fill the create form (name + QNZPE + productionType) and the new project appears in the list immediately below"
    - "User can click Reset to defaults and see the original 50 seed projects restored"
  artifacts:
    - path: "src/App.tsx"
      provides: "HashRouter wrapping Routes with root path"
      contains: "HashRouter"
    - path: "src/pages/ProjectListPage.tsx"
      provides: "Minimal project list page reading from store, with Reset button"
      contains: "useAppStore"
    - path: "src/components/CreateProjectForm.tsx"
      provides: "Form with name, QNZPE, productionType fields calling addProject"
      contains: "addProject"
    - path: "netlify.toml"
      provides: "Build config for Vite (command: npm run build, publish: dist)"
      contains: "[build]"
  key_links:
    - from: "src/pages/ProjectListPage.tsx"
      to: "src/store/useAppStore.ts"
      via: "useAppStore hook to read projects and call resetToDefaults"
      pattern: "useAppStore.*projects"
    - from: "src/App.tsx"
      to: "src/pages/ProjectListPage.tsx"
      via: "Route element rendering ProjectListPage"
      pattern: "Route.*ProjectListPage"
    - from: "src/components/CreateProjectForm.tsx"
      to: "src/store/useAppStore.ts"
      via: "useAppStore hook to call addProject"
      pattern: "useAppStore.*addProject"
---

<objective>
Install react-router-dom with HashRouter, create a minimal project list page with project creation form and reset button, configure Netlify, and deploy to verify the full data pipeline works end-to-end.

Purpose: Proves the entire Phase 2 data layer works in production: store loads seed data, persistence works, users can create projects, and the app is accessible on the web.
Output: Deployed Netlify site showing 50 seed projects with working create form and reset button.
</objective>

<execution_context>
@C:/Users/New User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/New User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer/02-CONTEXT.md
@.planning/phases/02-data-layer/02-RESEARCH.md

@src/App.tsx
@src/main.tsx
@src/store/useAppStore.ts

<interfaces>
<!-- From Plan 01 -->
From src/store/useAppStore.ts:
```typescript
export interface Project {
  id: string;
  isSeeded: boolean;
  inputs: ProjectInputs;
  createdAt: string;
}

// useAppStore.getState().projects: Project[]
// useAppStore.getState().addProject(inputs: ProjectInputs): void
// useAppStore.getState().resetToDefaults(): void
```

From src/scoring/types.ts:
```typescript
export interface ProjectInputs {
  projectName: string;
  productionType: 'film' | 'tv';
  qnzpe: number;
  // ... all other scoring fields default to zero/false
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-router-dom, create HashRouter shell, project list page with Reset button, and create project form</name>
  <files>package.json, src/App.tsx, src/pages/ProjectListPage.tsx, src/components/CreateProjectForm.tsx, netlify.toml</files>
  <action>
1. Install react-router-dom: `npm install react-router-dom`

2. Create `src/components/CreateProjectForm.tsx` — a minimal form component that:
   - Has three fields: project name (text input), QNZPE (number input), productionType (select: film/tv)
   - Validates on submit: name must not be empty, QNZPE must be > 0. Show inline error messages if validation fails
   - On valid submit, calls `useAppStore().addProject()` with a ProjectInputs object where all scoring fields default to zero/false/none, and only projectName, qnzpe, and productionType come from the form
   - Clears the form after successful submission
   - No styling beyond basic readability (Phase 3 handles UI design)

3. Create `src/pages/ProjectListPage.tsx` — a minimal page component that:
   - Reads `projects` from `useAppStore`
   - Renders a simple list: project name, productionType, and QNZPE formatted as dollars
   - Shows count: "Showing {N} projects"
   - Renders a "Reset to defaults" button that calls `useAppStore().resetToDefaults()` — per locked CONTEXT.md decision
   - Renders the `<CreateProjectForm />` component above or below the list
   - No styling beyond basic readability (Phase 3 handles UI design)
   - This is a verification page, not the final UI

4. Replace `src/App.tsx` with HashRouter scaffolding:
   ```tsx
   import { HashRouter, Routes, Route } from 'react-router-dom';
   import { ProjectListPage } from './pages/ProjectListPage';

   function App() {
     return (
       <HashRouter>
         <Routes>
           <Route path="/" element={<ProjectListPage />} />
         </Routes>
       </HashRouter>
     );
   }
   export default App;
   ```

5. Create `netlify.toml` at project root:
   ```toml
   [build]
     command = "npm run build"
     publish = "dist"
   ```
   No `[[redirects]]` needed — HashRouter handles all routing client-side.

6. Verify the build works: `npm run build` should succeed and produce `dist/index.html`.

7. Verify dev server works: `npm run dev` should show the project list with 50 seed projects.
  </action>
  <verify>
    <automated>cd "C:/Users/New User/Dropbox/claude/uplift" && npm run build && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - react-router-dom installed
    - App uses HashRouter with root route rendering ProjectListPage
    - ProjectListPage shows all projects from store with Reset to defaults button
    - CreateProjectForm renders with name, QNZPE, productionType fields and validates input
    - Submitting the form adds a new project that appears in the list immediately
    - netlify.toml configured for Vite build
    - npm run build succeeds
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Initialize git repo, push to GitHub, and deploy to Netlify</name>
  <files>.gitignore</files>
  <action>
Claude will prepare the repo for deployment:
1. Ensure `.gitignore` includes node_modules, dist, .env
2. Attempt `git init` if not already a git repo
3. Stage and commit all project files
4. Attempt `gh repo create uplift-compare --public --source=. --push` — if auth fails, present as checkpoint for user

The user then completes Netlify setup:
- Option A (CLI): `npx netlify-cli login` then `npx netlify-cli init` then `npx netlify-cli deploy --prod`
- Option B (Dashboard): app.netlify.com -> "Add new site" -> "Import from Git" -> select repo -> deploy
  </action>
  <verify>
    <automated>cd "C:/Users/New User/Dropbox/claude/uplift" && npm run build</automated>
  </verify>
  <done>
    - Code pushed to GitHub
    - Netlify site deployed and accessible
    - 50 projects visible on deployed site
    - Create form works: can add a project and see it in the list
    - Reset button works: restores original 50 seed projects
    - Page refresh preserves data (localStorage)
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `npx vitest run` — all tests pass
- Deployed Netlify site loads with 50 projects
- Create form adds a project to the list
- Reset button restores seed data
</verification>

<success_criteria>
- HashRouter renders project list at root route
- CreateProjectForm allows creating projects with name, QNZPE, and productionType
- Reset to defaults button restores original seed data
- App builds and deploys to Netlify
- 50 seed projects visible on deployed site
- Page refresh preserves data (localStorage persistence)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer/02-03-SUMMARY.md`
</output>

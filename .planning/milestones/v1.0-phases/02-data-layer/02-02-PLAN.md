---
phase: 02-data-layer
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/data/seedProjects.ts
  - src/data/__tests__/seedProjects.test.ts
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03]

must_haves:
  truths:
    - "50 seed projects exist with unique genre-evocative names"
    - "All seed projects have isSeeded=true and stable seed-NNN IDs"
    - "Roughly half pass the existing test, roughly half fail"
    - "Roughly half have QNZPE >= $100m, all have QNZPE >= $20m"
    - "All have hasSustainabilityPlan=true (A1 realism)"
    - "None have maoriCrewPercent > 0 or hasLeadCastMaori=true (C3/C10 realism)"
    - "B1 (hasStudioLease) is rare — only 3-5 of 50"
    - "Section E fields only on big-budget projects, max 5-8 of 50"
    - "Most projects (40+) have crewPercent >= 80"
    - "Mix of film and TV productionType"
    - "5-8 borderline projects near existing pass threshold (38-42pts)"
  artifacts:
    - path: "src/data/seedProjects.ts"
      provides: "50 typed Project objects as SEED_PROJECTS const array"
      exports: ["SEED_PROJECTS"]
      min_lines: 200
    - path: "src/data/__tests__/seedProjects.test.ts"
      provides: "Distribution and realism verification tests"
      min_lines: 50
  key_links:
    - from: "src/data/seedProjects.ts"
      to: "src/store/useAppStore.ts"
      via: "import Project type"
      pattern: "import.*Project.*from.*store/useAppStore"
    - from: "src/data/__tests__/seedProjects.test.ts"
      to: "src/scoring/index.ts"
      via: "import scoreExisting, scoreProposed for distribution verification"
      pattern: "import.*scoreExisting.*scoreProposed.*from.*scoring"
---

<objective>
Author 50 realistic seed projects as a static typed array and verify distribution constraints with automated tests.

Purpose: Provides the demonstration dataset that makes the app immediately useful on first load. Distribution verification ensures the comparison tool shows meaningful variety.
Output: seedProjects.ts with 50 projects, passing distribution and realism tests.
</objective>

<execution_context>
@C:/Users/New User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/New User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer/02-CONTEXT.md
@.planning/phases/02-data-layer/02-RESEARCH.md

@src/scoring/types.ts
@src/scoring/index.ts

<!-- Plan 01 creates these; executor reads them for the Project type -->
@src/store/useAppStore.ts
@src/data/seedProjects.ts

<interfaces>
<!-- From Plan 01 (will exist by wave 2) -->

From src/store/useAppStore.ts (after Plan 01):
```typescript
export interface Project {
  id: string;
  isSeeded: boolean;
  inputs: ProjectInputs;
  createdAt: string;
}
```

From src/scoring/index.ts:
```typescript
export { scoreExisting } from './scoreExisting';
export { scoreProposed } from './scoreProposed';
export type { ProjectInputs, ScoringResult, CriterionResult, SectionResult } from './types';
```

ProjectInputs fields needed for seed data (all must be set):
- projectName: string, qnzpe: number, productionType: 'film' | 'tv'
- hasSustainabilityPlan: boolean (always true)
- All other boolean/number/union fields (see types.ts for full list)
- castingLevel: 'none' | 'associate' | 'director'
- premiereType: 'none' | 'nz' | 'world'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write distribution verification tests</name>
  <files>src/data/__tests__/seedProjects.test.ts</files>
  <action>
Create `src/data/__tests__/seedProjects.test.ts` with tests that verify all distribution and realism constraints. These tests will fail until Task 2 populates the seed data. The test structure follows the pattern from RESEARCH.md code examples:

```typescript
import { SEED_PROJECTS } from '../seedProjects';
import { scoreExisting, scoreProposed } from '../../scoring';
```

Tests to include:

**Count and shape:**
- Has exactly 50 projects
- All have unique ids matching pattern `seed-NNN`
- All have isSeeded=true
- All have non-empty projectName
- Mix of productionType: at least 15 film, at least 15 TV

**Budget distribution:**
- All have qnzpe >= 20_000_000
- Between 20-30 have qnzpe >= 100_000_000

**Scoring distribution:**
- Between 20-30 pass scoreExisting (call scoreExisting(p.inputs).passed)
- At least 5 projects have existing score between 38-42 (borderline)

**Realism rules:**
- All have hasSustainabilityPlan === true
- All have maoriCrewPercent === 0
- All have hasLeadCastMaori === false
- Between 3-5 have hasStudioLease === true
- Section E fields (hasKnowledgeTransfer, commercialAgreementPercent > 0, infrastructureInvestment > 0) only on projects with qnzpe >= 100_000_000
- Max 5-8 projects have any Section E field active
- At least 40 projects have crewPercent >= 80

Run tests — they should fail (RED) since seedProjects.ts is currently an empty array stub.
  </action>
  <verify>
    <automated>cd "C:/Users/New User/Dropbox/claude/uplift" && npx vitest run src/data/__tests__/seedProjects.test.ts 2>&1 | head -5</automated>
  </verify>
  <done>Test file exists with all distribution and realism assertions. Tests fail because seed data is empty stub.</done>
</task>

<task type="auto">
  <name>Task 2: Author 50 seed projects and pass all distribution tests</name>
  <files>src/data/seedProjects.ts</files>
  <action>
Replace the stub `src/data/seedProjects.ts` with 50 fully populated `Project` objects.

**Approach:** Author all 50 projects inline as a typed const array. Each project must set EVERY field in ProjectInputs. Use genre-evocative names (sci-fi, thriller, drama, fantasy, action, comedy, horror). No NZ place names, no Maori words, no real franchise names.

**ID scheme:** `seed-001` through `seed-050`.
**createdAt:** All use `'2026-01-01T00:00:00.000Z'`.
**isSeeded:** All `true`.

**Distribution targets:**
- 25 projects with qnzpe $20m-$99m (small/mid budget)
- 25 projects with qnzpe $100m-$300m (big budget)
- ~25 passing existing test (>= 40pts with A1 met), ~25 failing
- 5-8 borderline at 38-42pts on existing test
- ~30 film, ~20 TV (approximate)

**Realism constraints (MANDATORY):**
- hasSustainabilityPlan: always true
- maoriCrewPercent: always 0
- hasLeadCastMaori: always false
- hasStudioLease: only 3-5 projects (rare)
- Section E (hasKnowledgeTransfer, commercialAgreementPercent, infrastructureInvestment): only on big-budget (>= $100m), max 5-8 projects total
- crewPercent >= 80 for at least 40 projects
- Big budget projects should have lower personnel counts (atlCount, btlKeyCount, castPercent inversely correlated with qnzpe)

**Critical:** After authoring, run `scoreExisting(p.inputs)` mentally or via test to ensure distribution targets are met. The scoring rules have non-obvious interactions — do NOT rely on manual point counting. Run the tests and iterate until they pass.

**Anti-pattern to avoid:** Do not store computed scores. Only store raw ProjectInputs. Scores are always recomputed.
  </action>
  <verify>
    <automated>cd "C:/Users/New User/Dropbox/claude/uplift" && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - 50 seed projects in seedProjects.ts with genre-evocative names
    - All distribution tests pass (count, budget split, scoring split, borderline cases)
    - All realism tests pass (A1, C3, C10, B1 rarity, Section E constraints, crew %)
    - All existing scoring tests still pass (productionType doesn't break anything)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` — full test suite green
- `npx tsc --noEmit` — no type errors
</verification>

<success_criteria>
- 50 seed projects committed as static typed array
- Distribution verified by automated tests: ~half pass existing, ~half >= $100m, all >= $20m
- Realism rules verified: A1 always true, C3/C10 never, B1 rare, Section E constrained
- Borderline cases exist near pass thresholds
- Mix of film and TV production types
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer/02-02-SUMMARY.md`
</output>

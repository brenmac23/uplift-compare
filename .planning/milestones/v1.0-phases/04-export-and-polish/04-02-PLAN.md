---
phase: 04-export-and-polish
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/components/PasswordGate.tsx
  - src/App.tsx
  - src/lib/criterionTooltips.ts
  - src/vite-env.d.ts
autonomous: false
requirements: [DISP-01]

must_haves:
  truths:
    - "On first load, user sees a full-screen password prompt before any app content"
    - "Entering the correct password reveals the app and persists for the browser tab session"
    - "Entering wrong password shows an error and clears the input"
    - "Closing and reopening the tab requires re-entering the password"
    - "Tooltip text is clear, complete, and explains each criterion in plain English"
  artifacts:
    - path: "src/components/PasswordGate.tsx"
      provides: "Full-screen password gate wrapping the app"
      exports: ["PasswordGate"]
    - path: "src/App.tsx"
      provides: "App root with PasswordGate wrapping HashRouter"
      contains: "PasswordGate"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/PasswordGate.tsx"
      via: "wraps router children"
      pattern: "<PasswordGate>"
    - from: "src/components/PasswordGate.tsx"
      to: "sessionStorage"
      via: "reads/writes unlock state"
      pattern: "sessionStorage"
    - from: "src/components/PasswordGate.tsx"
      to: "import.meta.env.VITE_APP_PASSWORD"
      via: "password comparison"
      pattern: "VITE_APP_PASSWORD"
---

<objective>
Add client-side password gate and review tooltip quality.

Purpose: Block casual access to the app with a simple password prompt (not true security) and ensure all criterion tooltips are clear and complete. The password gate wraps the entire app, and tooltip improvements enhance the existing CRITERION_TOOLTIPS map.
Output: PasswordGate.tsx component, modified App.tsx, improved tooltips, visual verification.
</objective>

<execution_context>
@C:/Users/New User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/New User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-export-and-polish/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/App.tsx (current):
```typescript
import { HashRouter, Routes, Route } from 'react-router-dom';
import { TooltipProvider } from '@/components/ui/tooltip';
import { NavBar } from '@/components/NavBar';
import { SummaryPage } from '@/pages/SummaryPage';
import { DetailPage } from '@/pages/DetailPage';

function App() {
  return (
    <HashRouter>
      <TooltipProvider>
        <NavBar />
        <Routes>
          <Route path="/" element={<SummaryPage />} />
          <Route path="/project/:id" element={<DetailPage />} />
        </Routes>
      </TooltipProvider>
    </HashRouter>
  );
}
export default App;
```

From src/lib/criterionTooltips.ts:
```typescript
export const CRITERION_TOOLTIPS: Record<string, string> = {
  // 33 existing + 24 proposed criterion tooltips
  // Keys: 'existing:A1', 'proposed:B3', etc.
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PasswordGate and wire into App.tsx</name>
  <files>src/components/PasswordGate.tsx, src/App.tsx, src/vite-env.d.ts</files>
  <action>
    1. Update src/vite-env.d.ts to extend ImportMetaEnv with `readonly VITE_APP_PASSWORD: string;` so TypeScript knows about the env var.

    2. Create src/components/PasswordGate.tsx:
       - Props: `{ children: React.ReactNode }`
       - Constants: `STORAGE_KEY = 'uplift-unlocked'`, `EXPECTED = import.meta.env.VITE_APP_PASSWORD ?? ''`
       - State: `unlocked` initialized lazily from `() => sessionStorage.getItem(STORAGE_KEY) === 'true'` (prevents flash)
       - State: `value` (input string), `error` (boolean)
       - If `EXPECTED` is empty string, skip the gate entirely (renders children) — allows local dev without env var
       - If unlocked, render children directly
       - Otherwise render full-screen centered password form:
         - White background covering entire viewport (`fixed inset-0 z-[100] flex items-center justify-center bg-white`)
         - App title "Uplift Compare" above the form
         - Password input field (type="password") with autofocus
         - Submit button "Enter"
         - If error: show "Incorrect password" in red text below input, clear input value
         - On submit: compare value to EXPECTED. If match, write to sessionStorage and set unlocked. If not, set error and clear value.
       - Use shadcn Button and basic HTML input (no need for shadcn Input unless already installed)

    3. Modify src/App.tsx:
       - Import PasswordGate
       - Wrap the entire HashRouter inside `<PasswordGate>...</PasswordGate>` so no routes render while locked
       - Structure: `<PasswordGate><HashRouter><TooltipProvider>...</TooltipProvider></HashRouter></PasswordGate>`

    4. Create .env.local (git-ignored) with `VITE_APP_PASSWORD=test` for local development. Add a comment in PasswordGate.tsx noting that the env var must be set in Netlify for production.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Password gate renders on app load, correct password reveals app, wrong password shows error, session persists via sessionStorage until tab close, empty VITE_APP_PASSWORD skips gate for local dev</done>
</task>

<task type="auto">
  <name>Task 2: Review and improve criterion tooltips</name>
  <files>src/lib/criterionTooltips.ts</files>
  <action>
    1. Read the full CRITERION_TOOLTIPS map in src/lib/criterionTooltips.ts.
    2. Review each tooltip for:
       - Clarity: Is the rule explained in plain English that a non-technical producer would understand?
       - Completeness: Are thresholds, point values, and conditions all mentioned?
       - Accuracy: Do the descriptions match the scoring spec (cross-reference with src/scoring/scoreExisting.ts and scoreProposed.ts)?
       - Consistency: Is the tone and structure consistent across tooltips?
       - Comparison: Where the existing and proposed tests differ for the same concept, does the tooltip note the difference?
    3. Make targeted improvements. Common issues to look for:
       - Missing point values or threshold numbers
       - Vague language ("some" instead of specific counts)
       - Missing "This criterion exists only in the [existing/proposed] test" notes for system-exclusive criteria
       - Inconsistent phrasing between similar criteria
    4. Do NOT rewrite tooltips that are already clear and complete — minimal changes only.
    5. Run tests to ensure nothing breaks (tooltips are used by key lookups).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All tooltips reviewed, unclear or incomplete ones improved, no tooltip removed or renamed (keys unchanged), all tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify password gate, export, and tooltips</name>
  <action>
    Human verifies the complete Phase 4 feature set: password gate, Excel export (from Plan 01), and tooltip quality.
  </action>
  <what-built>Password gate blocking app access, Export button in NavBar, tooltip improvements</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in a NEW browser tab (or clear sessionStorage first)
    2. Verify: Full-screen password prompt appears with "Uplift Compare" title
    3. Enter wrong password — verify error message appears
    4. Enter correct password ("test" if using .env.local) — verify app loads
    5. Refresh the page — verify app loads without re-prompting (sessionStorage persists)
    6. Verify: Export button visible in NavBar next to Import button
    7. Click Export — verify .xlsx file downloads with today's date in filename
    8. Open .xlsx in Excel/Google Sheets — verify headers and data look correct
    9. Navigate to Detail page — hover over criterion labels to check tooltip quality
    10. Close browser tab, reopen — verify password is required again
  </how-to-verify>
  <verify>Human confirms all 10 verification steps pass</verify>
  <done>Password gate, export, and tooltips all verified by human</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm test` passes with all existing and any new tests
- Password gate blocks app until correct password entered
- Password persists in sessionStorage (survives refresh, not tab close)
- Export button works and produces valid .xlsx
- Tooltips are clear and complete
</verification>

<success_criteria>
- App is access-controlled via client-side password gate
- Password is configured via VITE_APP_PASSWORD env var (not hardcoded)
- Tooltips are reviewed and improved where needed
- Human verification confirms all features work correctly together
</success_criteria>

<output>
After completion, create `.planning/phases/04-export-and-polish/04-02-SUMMARY.md`
</output>

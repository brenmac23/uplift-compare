---
phase: 02-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scoring/types.ts
  - src/store/useAppStore.ts
  - src/store/__tests__/useAppStore.test.ts
  - src/data/seedProjects.ts
autonomous: true
requirements: [PROJ-04]

must_haves:
  truths:
    - "ProjectInputs includes productionType field with 'film' | 'tv' union"
    - "Store holds a projects array of Project objects"
    - "addProject creates a user project with isSeeded=false and a UUID id"
    - "updateProject modifies any project's inputs by id"
    - "deleteProject removes user-created projects but ignores seed projects"
    - "resetToDefaults replaces projects array with SEED_PROJECTS import"
    - "Schema migrates from v1 to v2 by injecting seed projects"
  artifacts:
    - path: "src/scoring/types.ts"
      provides: "productionType field on ProjectInputs"
      contains: "productionType"
    - path: "src/store/useAppStore.ts"
      provides: "Project type, CRUD actions, persist v2 with migrate"
      exports: ["useAppStore", "Project"]
    - path: "src/store/__tests__/useAppStore.test.ts"
      provides: "Tests for CRUD actions, seed protection, migration"
    - path: "src/data/seedProjects.ts"
      provides: "Stub file for SEED_PROJECTS (replaced by Plan 02)"
      exports: ["SEED_PROJECTS"]
  key_links:
    - from: "src/store/useAppStore.ts"
      to: "src/scoring/types.ts"
      via: "import ProjectInputs"
      pattern: "import.*ProjectInputs.*from.*scoring"
    - from: "src/store/useAppStore.ts"
      to: "src/data/seedProjects.ts"
      via: "import SEED_PROJECTS for resetToDefaults and migrate"
      pattern: "import.*SEED_PROJECTS.*from.*data/seedProjects"
---

<objective>
Expand the Zustand store with a Project entity, CRUD actions, and schema migration from v1 to v2. Add `productionType` to `ProjectInputs`.

Purpose: Establishes the data model and store contract that seed data (Plan 02) and UI (Phase 3) build against.
Output: Updated types.ts, fully functional useAppStore with Project CRUD, passing tests.
</objective>

<execution_context>
@C:/Users/New User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/New User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-layer/02-CONTEXT.md
@.planning/phases/02-data-layer/02-RESEARCH.md

@src/scoring/types.ts
@src/store/useAppStore.ts
@src/store/__tests__/useAppStore.test.ts
@src/scoring/index.ts

<interfaces>
<!-- Existing types the executor needs -->

From src/scoring/types.ts:
```typescript
export interface ProjectInputs {
  projectName: string;
  qnzpe: number;
  hasSustainabilityPlan: boolean;
  // ... all scoring fields (see full file)
  castingLevel: 'none' | 'associate' | 'director';
  premiereType: 'none' | 'nz' | 'world';
  // etc.
}
```

From src/store/useAppStore.ts (current skeleton):
```typescript
interface AppState {
  schemaVersion: number;
}
// persist config: name 'uplift-storage', version: 1
```

From src/scoring/index.ts:
```typescript
export { scoreExisting } from './scoreExisting';
export { scoreProposed } from './scoreProposed';
export type { ProjectInputs, ScoringResult, CriterionResult, SectionResult } from './types';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add productionType to ProjectInputs and define Project entity</name>
  <files>src/scoring/types.ts, src/store/useAppStore.ts, src/data/seedProjects.ts</files>
  <behavior>
    - Test: ProjectInputs has productionType field accepting 'film' | 'tv'
    - Test: Project interface has id (string), isSeeded (boolean), inputs (ProjectInputs), createdAt (string)
    - Test: Store initialises with schemaVersion: 2 and a projects array
    - Test: addProject(inputs) appends a Project with isSeeded=false, a UUID id, and ISO createdAt
    - Test: updateProject(id, inputs) replaces inputs for matching project
    - Test: deleteProject(id) removes project when isSeeded=false
    - Test: deleteProject(id) does NOT remove project when isSeeded=true
    - Test: resetToDefaults() sets projects back to SEED_PROJECTS
    - Test: migrate from v1 persisted state produces v2 state with projects array
  </behavior>
  <action>
1. Add `productionType: 'film' | 'tv'` field to `ProjectInputs` in `src/scoring/types.ts`, placed after `projectName` in the Identity section. Add JSDoc: "Production type — film or TV series."

2. In `src/store/useAppStore.ts`:
   - Export a `Project` interface: `{ id: string; isSeeded: boolean; inputs: ProjectInputs; createdAt: string; }`
   - Expand `AppState` to include: `projects: Project[]`, `addProject`, `updateProject`, `deleteProject`, `resetToDefaults`
   - Import `SEED_PROJECTS` from `../data/seedProjects` (this file won't exist yet — create a temporary stub: `export const SEED_PROJECTS: Project[] = [];` in `src/data/seedProjects.ts` so TypeScript compiles)
   - Set initial state: `schemaVersion: 2, projects: SEED_PROJECTS`
   - addProject: use `crypto.randomUUID()` for id, `isSeeded: false`, `new Date().toISOString()` for createdAt
   - updateProject: map over projects, replace `inputs` for matching id
   - deleteProject: filter out only if `!p.isSeeded`
   - resetToDefaults: `set({ projects: SEED_PROJECTS })`
   - Bump persist version to 2, add `migrate` function: if version === 1, return `{ ...persistedState, schemaVersion: 2, projects: SEED_PROJECTS }`

3. Create stub file `src/data/seedProjects.ts` with: `import type { Project } from '../store/useAppStore'; export const SEED_PROJECTS: Project[] = [];` — Plan 02 replaces this with real data.

4. Update existing store tests in `src/store/__tests__/useAppStore.test.ts` and add new tests for all CRUD actions and migration. Use `useAppStore.persist.clearStorage()` + `rehydrate()` in beforeEach. For migration test: manually set localStorage with v1 shape, then rehydrate and verify projects array appears.

5. Update any existing test fixtures that create `ProjectInputs` objects to include `productionType: 'film'`.
  </action>
  <verify>
    <automated>cd "C:/Users/New User/Dropbox/claude/uplift" && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - productionType field exists on ProjectInputs
    - Project interface exported from useAppStore
    - All CRUD actions work: add, update, delete (seed-protected), resetToDefaults
    - Persist v2 with migrate from v1
    - All tests pass (existing scoring tests still green with productionType added)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass (scoring + store)
- TypeScript compiles: `npx tsc --noEmit`
</verification>

<success_criteria>
- ProjectInputs has productionType field
- Project entity with id, isSeeded, inputs, createdAt is exported
- Store CRUD actions work correctly with seed protection
- Schema migration from v1 to v2 works
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-layer/02-01-SUMMARY.md`
</output>

---
phase: 01-scoring-engine
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/store/useAppStore.ts
  - src/store/__tests__/useAppStore.test.ts
  - src/scoring/__tests__/sharedInputs.test.ts
  - src/scoring/index.ts
autonomous: true
requirements:
  - SCORE-04
  - SCORE-06
  - INFRA-02

must_haves:
  truths:
    - "Both scoreExisting() and scoreProposed() accept the same ProjectInputs type — changing a shared field affects both outputs"
    - "Zustand store initialises with schemaVersion: 1 on fresh localStorage"
    - "App boots without crashing when localStorage is empty"
    - "Both scoring results contain per-criterion detail with id, label, score, maxScore"
    - "Scoring functions are re-exported from a single index barrel file"
  artifacts:
    - path: "src/store/useAppStore.ts"
      provides: "Zustand store skeleton with persist middleware"
      exports: ["useAppStore"]
    - path: "src/store/__tests__/useAppStore.test.ts"
      provides: "Smoke tests for store initialisation"
    - path: "src/scoring/__tests__/sharedInputs.test.ts"
      provides: "Integration tests verifying both engines consume shared inputs"
    - path: "src/scoring/index.ts"
      provides: "Barrel export for scoring module"
      exports: ["scoreExisting", "scoreProposed", "ProjectInputs", "ScoringResult", "CriterionResult"]
  key_links:
    - from: "src/scoring/__tests__/sharedInputs.test.ts"
      to: "src/scoring/scoreExisting.ts"
      via: "imports scoreExisting"
      pattern: "import.*scoreExisting"
    - from: "src/scoring/__tests__/sharedInputs.test.ts"
      to: "src/scoring/scoreProposed.ts"
      via: "imports scoreProposed"
      pattern: "import.*scoreProposed"
    - from: "src/store/useAppStore.ts"
      to: "zustand/middleware"
      via: "persist middleware with version"
      pattern: "persist.*version.*1"
---

<objective>
Create the Zustand store skeleton with localStorage persistence, write shared-input integration tests proving both engines consume the same ProjectInputs, and create the barrel export for the scoring module.

Purpose: Complete INFRA-02 (store boots on fresh localStorage) and SCORE-04 (shared inputs feed both systems). This plan ties together the two independently-built scoring engines and sets up the persistence foundation for Phase 2.

Output: Working Zustand store, shared input integration tests, and a clean public API for the scoring module.
</objective>

<execution_context>
@C:/Users/New User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/New User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scoring-engine/1-CONTEXT.md
@.planning/phases/01-scoring-engine/01-RESEARCH.md
@.planning/phases/01-scoring-engine/01-01-SUMMARY.md
@.planning/phases/01-scoring-engine/01-02-SUMMARY.md
@.planning/phases/01-scoring-engine/01-03-SUMMARY.md

Source code (created by Plans 01-03):
@src/scoring/types.ts
@src/scoring/scoreExisting.ts
@src/scoring/scoreProposed.ts
@src/scoring/helpers.ts
@src/scoring/spec.ts
</context>

<interfaces>
<!-- From Plans 01-03 — executor uses these directly -->

From src/scoring/types.ts:
- ProjectInputs: all raw production input fields (single type for both engines)
- ScoringResult: { totalPoints, maxPoints, passed, mandatoryMet, passThreshold, criteria }
- CriterionResult: { id, label, score: number | 'N/A', maxScore, mandatory?, mandatoryMet? }

From src/scoring/scoreExisting.ts:
- scoreExisting(inputs: ProjectInputs): ScoringResult

From src/scoring/scoreProposed.ts:
- scoreProposed(inputs: ProjectInputs): ScoringResult
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zustand store skeleton with persist middleware</name>
  <files>src/store/useAppStore.ts, src/store/__tests__/useAppStore.test.ts</files>
  <behavior>
    - Store initialises with schemaVersion: 1 when no localStorage exists
    - useAppStore.getState().schemaVersion === 1
    - Store survives a clear-and-reinit cycle (simulating fresh browser)
    - persist version is set to 1 for future migration support
  </behavior>
  <action>
**Write tests first** in `src/store/__tests__/useAppStore.test.ts`:

1. Test that `useAppStore.getState().schemaVersion` equals 1
2. Test that after clearing the persist storage and reinitialising, schemaVersion is still 1 (default kicks in)
3. Test the store type has schemaVersion as a number

Note: For testing Zustand stores outside React, use `useAppStore.getState()` directly — no need for React rendering. For simulating fresh localStorage, use `useAppStore.persist.clearStorage()` then `useAppStore.persist.rehydrate()`.

**Then implement** `src/store/useAppStore.ts`:

```typescript
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';

interface AppState {
  schemaVersion: number;
  // Phase 2 will add: projects, addProject, updateProject, etc.
}

export const useAppStore = create<AppState>()(
  persist(
    () => ({
      schemaVersion: 1,
    }),
    {
      name: 'uplift-storage',
      version: 1,
      storage: createJSONStorage(() => localStorage),
      // Phase 2 will add: migrate, partialize
    }
  )
);
```

Keep the store minimal — only `schemaVersion` for now. Phase 2 adds the projects array and actions.
  </action>
  <verify>
    <automated>cd "C:/Users/New User/Dropbox/claude/uplift" && npx vitest run src/store/__tests__/useAppStore.test.ts</automated>
  </verify>
  <done>Store exports useAppStore, initialises with schemaVersion: 1, tests pass including fresh-localStorage simulation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared inputs integration tests and barrel export</name>
  <files>src/scoring/__tests__/sharedInputs.test.ts, src/scoring/index.ts</files>
  <behavior>
    - Given identical ProjectInputs, both scoreExisting() and scoreProposed() return results without error
    - Changing qnzpe affects both engines' passThreshold/verdict
    - Changing crewPercent affects crew scoring in both engines (existing C2 and proposed B2)
    - Changing castPercent affects cast scoring in both engines (existing C1 and proposed B1)
    - Changing vfxPercent produces different scores due to different thresholds (existing 50/75/90 vs proposed 30/50/75)
    - Both results contain criteria arrays with proper CriterionResult shape
    - Both results have correct maxPoints (85 vs 70)
  </behavior>
  <action>
**Write tests first** in `src/scoring/__tests__/sharedInputs.test.ts`:

1. Create a baseline `ProjectInputs` object with all fields populated to reasonable values
2. Test: Both `scoreExisting(baseline)` and `scoreProposed(baseline)` return valid ScoringResult objects
3. Test: `result.criteria` is a non-empty array and each entry has id, label, score, maxScore
4. Test: Changing `crewPercent` from 50 to 85 changes crew criterion score in BOTH results
5. Test: Changing `castPercent` from 50 to 85 changes cast criterion score in BOTH results
6. Test: `vfxPercent` of 60 produces different scores in each engine (existing: 1pt at 50% tier; proposed: 2pts at 50% tier)
7. Test: `qnzpe` of 50_000_000 gives proposed passThreshold 20; `qnzpe` of 150_000_000 gives passThreshold 30
8. Test: Both engines accept the SAME input object (TypeScript compile check — if this compiles, SCORE-04 is satisfied)

**Then create barrel export** in `src/scoring/index.ts`:

```typescript
export { scoreExisting } from './scoreExisting';
export { scoreProposed } from './scoreProposed';
export type { ProjectInputs, ScoringResult, CriterionResult, SectionResult } from './types';
```

This gives downstream consumers (Phase 2 seed data, Phase 3 UI) a single clean import path.
  </action>
  <verify>
    <automated>cd "C:/Users/New User/Dropbox/claude/uplift" && npx vitest run src/scoring/__tests__/sharedInputs.test.ts && npx vitest run</automated>
  </verify>
  <done>
- sharedInputs.test.ts passes proving both engines consume identical ProjectInputs
- Threshold divergence verified (vfxPercent produces different scores per engine)
- index.ts barrel export exists and re-exports all public API
- Full test suite (npx vitest run) passes with zero failures
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite passes (all 4 test files: helpers, scoreExisting, scoreProposed, sharedInputs, useAppStore)
- `useAppStore.getState().schemaVersion === 1` in test
- Both engines accept same ProjectInputs — TypeScript compiles
- Shared field changes propagate to both engine outputs
- `src/scoring/index.ts` re-exports the full public API
</verification>

<success_criteria>
- Zustand store boots on fresh localStorage without crash (INFRA-02)
- Both scoring engines consume shared ProjectInputs (SCORE-04)
- Both results contain per-criterion detail (SCORE-06)
- Full test suite green — all scoring logic validated
- Clean barrel export ready for Phase 2 and Phase 3 consumption
</success_criteria>

<output>
After completion, create `.planning/phases/01-scoring-engine/01-04-SUMMARY.md`
</output>

---
phase: 01-scoring-engine
plan: 03
type: tdd
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/scoring/scoreProposed.ts
  - src/scoring/__tests__/scoreProposed.test.ts
autonomous: true
requirements:
  - SCORE-02
  - SCORE-05

must_haves:
  truths:
    - "scoreProposed() returns correct total for a known passing input set (manually calculated)"
    - "scoreProposed() applies tiered pass threshold: 20 for QNZPE < $100m, 30 for QNZPE >= $100m"
    - "scoreProposed() returns passed: true for 25pts with QNZPE $50m (threshold 20)"
    - "scoreProposed() returns passed: false for 25pts with QNZPE $150m (threshold 30)"
    - "scoreProposed() returns 70 as maxPoints"
    - "scoreProposed() returns mandatoryMet: true always (no mandatory criteria in proposed)"
    - "Every criterion in the result has an id, label, score (number or N/A), and maxScore"
    - "Existing-only criteria (A1 Sustainability etc.) do NOT appear in proposed results"
    - "Proposed B5 half-point arithmetic works: 3 persons = 1.5pts, 8 persons = 4pts (capped)"
  artifacts:
    - path: "src/scoring/scoreProposed.ts"
      provides: "scoreProposed pure function"
      exports: ["scoreProposed"]
      min_lines: 60
    - path: "src/scoring/__tests__/scoreProposed.test.ts"
      provides: "Unit tests for proposed scoring engine"
      min_lines: 100
  key_links:
    - from: "src/scoring/scoreProposed.ts"
      to: "src/scoring/types.ts"
      via: "imports ProjectInputs, ScoringResult, CriterionResult"
      pattern: "import.*from.*types"
    - from: "src/scoring/scoreProposed.ts"
      to: "src/scoring/helpers.ts"
      via: "uses scoreHighestTier, scoreCountCapped"
      pattern: "import.*scoreHighestTier.*scoreCountCapped"
    - from: "src/scoring/scoreProposed.ts"
      to: "src/scoring/spec.ts"
      via: "reads tier arrays and point values from PROPOSED_SPEC"
      pattern: "import.*PROPOSED_SPEC"
---

<objective>
Implement `scoreProposed()` using TDD — write failing tests first from SCORING_SPEC.md, then implement the function to make them pass.

Purpose: Encode the proposed NZ Screen Production Rebate 5% Uplift points test (70 max, tiered pass threshold by QNZPE) as a pure function validated by unit tests derived from the human-verified spec.

Output: Fully tested `scoreProposed()` function covering all 4 sections (A-D), ~22 criteria, tiered pass logic, and half-point BTL arithmetic.
</objective>

<execution_context>
@C:/Users/New User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/New User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-scoring-engine/1-CONTEXT.md
@.planning/phases/01-scoring-engine/01-RESEARCH.md
@.planning/phases/01-scoring-engine/SCORING_SPEC.md
@.planning/phases/01-scoring-engine/01-01-SUMMARY.md

Source types and helpers (created by Plan 01):
@src/scoring/types.ts
@src/scoring/helpers.ts
@src/scoring/spec.ts
</context>

<interfaces>
<!-- From Plan 01 — executor uses these directly -->

From src/scoring/types.ts:
- ProjectInputs: all raw production input fields
- ScoringResult: { totalPoints, maxPoints, passed, mandatoryMet, passThreshold, criteria }
- CriterionResult: { id, label, score: number | 'N/A', maxScore, mandatory?, mandatoryMet? }

From src/scoring/helpers.ts:
- scoreHighestTier(tiers: Array<[number, number]>, value: number): number
- scoreCountCapped(count: number, pointsEach: number, cap: number): number

From src/scoring/spec.ts:
- PROPOSED_SPEC: all tier arrays, point values, caps for the proposed test
</interfaces>

<feature>
  <name>scoreProposed — Proposed Uplift Points Test Engine</name>
  <files>src/scoring/scoreProposed.ts, src/scoring/__tests__/scoreProposed.test.ts</files>
  <behavior>
    ## Pass/Fail Logic (QNZPE-tiered threshold)
    - QNZPE < $100m (< 100_000_000): passThreshold = 20
    - QNZPE >= $100m (>= 100_000_000): passThreshold = 30
    - Total >= passThreshold → passed: true
    - Total < passThreshold → passed: false
    - mandatoryMet: always true (no mandatory criteria in proposed)
    - maxPoints always 70
    - Edge case: QNZPE exactly 99_999_999 → threshold 20; exactly 100_000_000 → threshold 30

    ## Section A: NZ Production Activity (20pts max)
    - A1: hasPreviousQNZPE → 2pts (binary)
    - A2: hasAssociatedContent → 2pts (binary, 5yr window — input is boolean)
    - A3: shootingNZPercent → tiered [[90, 2], [75, 1]]
    - A4: regionalPercent → tiered [[25, 2], [10, 1]] (NOTE: two tiers, existing only has 25%)
    - A5: picturePostPercent → tiered [[75, 3], [50, 2], [30, 1]]
    - A6: soundPostPercent → tiered [[75, 3], [50, 2], [30, 1]]
    - A7: vfxPercent → tiered [[75, 3], [50, 2], [30, 1]] (NOTE: different from existing [[90, 3], [75, 2], [50, 1]])
    - A8: conceptPhysicalPercent → tiered [[75, 3], [50, 2], [30, 1]] (NOTE: different from existing)

    ## Section B: NZ Personnel (32pts max)
    - B1: castPercent → tiered [[80, 3], [60, 2]] (NOTE: two tiers, existing only has 80% → 2pts)
    - B2: crewPercent → tiered [[80, 3]] (NOTE: 3pts not 1pt like existing)
    - B3: atlCount → countCapped(count, 3, 9)
    - B4: btlKeyCount → countCapped(count, 1, 4)
    - B5: btlAdditionalCount → countCapped(count, 0.5, 4) (HALF POINT: 0.5pts each, cap 4pts, so up to 8 persons)
    - B6: hasLeadCast → 4pts (binary) (NOTE: 4pts not 3pts like existing)
    - B7: supportingCastCount → countCapped(count, 1, 3)
    - B8: castingLevel → none=0, associate=1, director=2

    ## Section C: Skills (6pts max)
    - C1: hasIndustrySeminars → 1pt
    - C2: hasEdSeminars → 1pt
    - C3: attachmentCount → tiered by QNZPE band (from spec)
    - C4: internshipCount → tiered by QNZPE band (from spec)

    ## Section D: Marketing (12pts max)
    - D1: hasNZPremiere (2pts) + hasIntlPromotion (2pts) → can earn both (max 4pts)
    - D2: hasFilmMarketing → 3pts
    - D3: hasLocationAnnouncement → 1pt
    - D4: hasTourismPartnership → 4pts (NOTE: 4pts not 3pts like existing)

    ## Full Worked Example Tests
    - Use the worked examples from SCORING_SPEC.md
    - At least one known passer and one known failer with exact expected totals
    - Separate tests for sub-$100m and over-$100m QNZPE to verify both thresholds
  </behavior>
  <implementation>
Create `src/scoring/scoreProposed.ts` exporting a single pure function:

```typescript
export function scoreProposed(inputs: ProjectInputs): ScoringResult
```

Implementation approach:
1. Score each criterion using helpers and spec constants from PROPOSED_SPEC
2. Build CriterionResult array — all criteria have mandatory: undefined (no mandatory in proposed)
3. Sum numeric scores
4. Determine passThreshold from QNZPE: `inputs.qnzpe >= 100_000_000 ? 30 : 20`
5. Apply pass logic: totalPoints >= passThreshold
6. Return ScoringResult with mandatoryMet: true (always), passThreshold as computed

Key differences from existing engine to be careful about:
- B5 uses 0.5pts per person (not 1pt) — use scoreCountCapped(count, 0.5, 4)
- B1 Cast has two tiers (60% and 80%), not just one
- B2 Crew awards 3pts (not 1pt)
- A7 VFX thresholds are 30/50/75% (not 50/75/90%)
- A8 Concept/Physical thresholds are 30/50/75% (not 50/75/90%)
- A4 Regional has a 10% tier that existing B5 does not have
- D1 Premiere is two independent booleans, not a single selector
- D4 Tourism Partnership is 4pts (not 3pts)
- B6 Lead Cast is 4pts (not 3pts)

Do NOT include existing-only criteria. The proposed engine only returns criteria that exist in the proposed test.

Reference PROPOSED_SPEC from spec.ts for all numeric values — no magic numbers.
  </implementation>
</feature>

<verification>
- `npx vitest run src/scoring/__tests__/scoreProposed.test.ts` — all tests pass
- All ~22 criteria produce correct scores for known inputs
- Pass threshold correctly branches on QNZPE (tested at boundary: 99_999_999 vs 100_000_000)
- Section totals verified: A=20, B=32, C=6, D=12 (total=70)
- B5 half-point arithmetic verified: 1 person=0.5, 3 persons=1.5, 8 persons=4 (capped)
- Worked example from SCORING_SPEC.md produces exact expected total
</verification>

<success_criteria>
- scoreProposed() is a pure function with no side effects
- All tests pass with values derived from SCORING_SPEC.md
- Tiered pass threshold works: 20 for <$100m, 30 for >=$100m QNZPE
- mandatoryMet is always true (no mandatory criteria in proposed)
- Half-point BTL scoring works correctly
- maxPoints = 70
</success_criteria>

<output>
After completion, create `.planning/phases/01-scoring-engine/01-03-SUMMARY.md`
</output>
